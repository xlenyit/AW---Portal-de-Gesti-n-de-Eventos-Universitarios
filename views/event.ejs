<!DOCTYPE html>
<html lang="es">
    <%-include('partes/head') %>
    <body>
        <div class="container ">
            <div class="d-flex mh-0 ph-0">
                <div id="mainDiv" class=" ">
                    <h1><%= nombre %></h1>
                    <div class="container vw-100 m-0 p-0">
                        <div class="row">
                            <div class="col" id="eventImageContainer">
                                <img src="<%=image_path%>" alt="<%=image_alt%>" class="w-100">
                            </div>
                            <div class="col" id="eventSpecificationsContainer">
                                <p>Fecha: <%= fecha %></p>
                                <p>Hora: <%= hora %></p>
                                <p>Ubicación: <%= ubicacion %></p>
                                <p>Nombre: <%= precio%></p>
                                <p>Capacidad: <%= capacidad_maxima%></p>
                            </div>
                        </div>
                        <div class="container vw-100 m-0 p-0">

                            <div class="row" id="joinContainer">
                                <div id="ocupacionContainer" class="col">
                                    <span>Ocupación: <%= ocupacion%> / <%= capacidad_maxima%></span>
                                </div>
                                <div id="botonIncribirseContainer" class="col">
                                    <!--- La idea de esto es luego poder cambiarle el color a cada botón -->
                                    <% if (!usuario.esta_inscrito) {%>
                                        <button class="w-50">Inscribirse</button>
                                    <% } else if (usuario.esta_lista_espera) {%>
                                        <button class="w-50">Quitarse de lista de espera</button>
                                    <% } else{%>
                                        <button class="w-50">Desapuntarse</button>
                                    <%}; %>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div id="descripcion" class="mt-3 p-2 border-solid border">
                        <%= descripcion %>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>